# Backend

This directory will contain the Spring Boot backend API, including controllers, services, persistence code, configuration, and backend tests.

The Spring Boot project has intentionally not been created yet. Authentication and PostgreSQL integration will be added in a later development step.
