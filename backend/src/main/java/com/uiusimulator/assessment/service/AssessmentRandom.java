package com.uiusimulator.assessment.service;
/** Separate injectable seam; tests force success/caught without exposing a client override. */
public interface AssessmentRandom { double nextDouble(); }
