# Unity Client

This directory will contain the Unity 6 3D game project, including scenes, scripts, prefabs, materials, assets, and client-side configuration.

The Unity project has intentionally not been created yet. Keep generated Unity folders such as `Library/`, `Temp/`, and `Builds/` out of version control according to the repository `.gitignore`.
